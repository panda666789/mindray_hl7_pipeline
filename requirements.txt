# Full deployment (PhysRecorder GUI + all devices)
bleak>=0.21.0
pyserial>=3.5
hidapi>=0.14.0
opencv-python>=4.8.0
# PyCameraList>=0.1.0  # 可选，摄像头列表功能，PyPI 上可能不可用
requests>=2.28.0
sv-ttk>=2.6.0

# Testing
pytest>=7.4.0
